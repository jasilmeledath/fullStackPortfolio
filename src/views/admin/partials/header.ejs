<header class="border-b border-green-500/30 bg-black/80 backdrop-blur-sm sticky top-0 z-50">
    <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
            <div class="flex items-center space-x-4">
                <div class="w-8 h-8 bg-green-500 rounded-full animate-pulse"></div>
                <h1 class="text-xl font-bold text-green-400 glitch-text" data-text="ADMIN_PANEL">ADMIN_PANEL</h1>
            </div>
            <div class="flex items-center space-x-4">
                <div class="text-sm">
                    <span class="text-green-500">USER:</span>
                    <span class="typing-text" data-text="<%= user.username %>"></span>
                </div>
                <button id="logout-btn" class="px-4 py-2 bg-red-600/20 border border-red-500 text-red-400 hover:bg-red-600/40 transition-all duration-300 rounded">
                    <i class="fas fa-sign-out-alt mr-2"></i>LOGOUT
                </button>
            </div>
        </div>
    </div>
</header>